<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.unipi.dii.mecperfng">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Results.BandwidthTCP_Results" android:theme="@style/AppTheme.MyStyle"></activity>
        <activity
            android:name=".Results.BandwidthUDP_Results"
            android:theme="@style/AppTheme.MyStyle" />
        <activity
            android:name=".Results.Rtt_Results"
            android:theme="@style/AppTheme.MyStyle" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.MyStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- android:usesCleartextTraffic="true" -->
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>